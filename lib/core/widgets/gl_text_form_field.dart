import 'package:flutter/material.dart';

class GlTextFormField extends StatelessWidget {
  const GlTextFormField({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 59,
      decoration: const BoxDecoration(),
    );
  }
}